# Copy this file to .env and fill in real values.
# .env is gitignored - never commit real secrets.

# Grafana admin password. Use a long random string on the VM:
#   openssl rand -base64 32
GRAFANA_ADMIN_PASSWORD=change-me-before-docker-compose-up
